- User asked to unzip and deploy `/opt/hr2.0.zip`; project extracted to `/opt/hr2.0/hr2.0`.
- Deployment task tracking used JJC-20260322-001 (initial deploy, then cancelled on user request) and JJC-20260322-002 (redeploy and ongoing repair).
- Frontend build issues were repaired enough to pass: invalid Element Plus icon imports (`Gift`, `Start`, `Heart`) and multiple unused-variable TypeScript errors in frontend Vue files.
- Backend compile/runtime issues encountered during redeploy:
  - Missing mapper scanning caused `SalaryCalculationDao` injection failure; `HRApplication.java` was updated with `@MapperScan("com.hr.dao")`.
  - Recruitment-related backend service/controller interfaces were patched to align methods so Maven build could pass (`ResumeService*`, `InterviewRecordService*`, corresponding controllers).
  - Runtime failure persists in `hr-app`: `java.lang.IllegalArgumentException: Invalid value type for attribute 'factoryBeanObjectType': java.lang.String` under Spring Boot 3.2.4 / Spring 6.1.5.
- Dependency investigation findings:
  - `mybatis-plus` upgraded from 3.5.5 to 3.5.7.
  - `flowable` upgraded from 6.8.0 to 7.0.1, but the same runtime error persisted.
  - Latest mitigation in progress before compaction: temporarily removed the Flowable dependency from backend `pom.xml` and started rebuilding `hr-app` to isolate whether Flowable is the cause.
- User explicitly granted ongoing approval to continue deployment-fix work without re-asking: "继续，批准，后续都批准".
