- User requested installation of several ClawHub skills. Successfully installed: `agent-browser-clawdbot` at `skills/agent-browser-clawdbot`, `proactive-agent` at `skills/proactive-agent`, `auto-updater` at `skills/auto-updater`, and `playwright-mcp` at `skills/playwright-mcp`.
- `elite-longterm-memory` was inspected but flagged as suspicious by ClawHub/VirusTotal and was not installed; installing it would require explicit force approval.
- On the `edict` dashboard deployment, the public no-port URL in use is `https://txy.960898.xyz/`, with local service on `127.0.0.1:7891` behind Nginx.
- `edict` auth/login system was heavily customized: default login username changed from `emperor` to `admin`, password remains `123456` unless changed by user, while role stays `emperor` in `edict_auth.json`.
- Login UI changes completed: removed any footer/path hint exposing `edict_auth.json`; enlarged login page layout for desktop screens; added top-right `个人设置` menu containing password change and logout.
- Chromium and chromium-driver were installed via apt to enable headless browser screenshots on the host.
